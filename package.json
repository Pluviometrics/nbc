{
  "name": "nbc-stormwater-tools",
  "private": true,
  "version": "1.0.0",
  "description": "NBC Stormwater packaging + relining tool — single-page web app",
  "type": "module",
  "scripts": {
    "build": "node scripts/prebuild.mjs",
    "lint": "eslint .",
    "serve": "npx http-server -p 5173 -c-1 .",
    "test": "npm run validate",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "validate": "node scripts/validate-repo.mjs"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "@playwright/test": "^1.45.0"
  }
}
